let should = require('should');

let VirtualRecord = require('../lib/virtual-record');

describe('VirtualRecord', ()=> {

	it('should be a function', ()=> {
		should(VirtualRecord).be.a.Function();
	});

});